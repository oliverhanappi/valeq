namespace Valeq.Configuration
{
    public enum PropertySearchScope
    {
        /// <summary>
        /// Takes only public properties into account.
        /// </summary>
        OnlyPublic,
        
        /// <summary>
        /// Takes all properties into account.
        /// </summary>
        All,
    }
}
